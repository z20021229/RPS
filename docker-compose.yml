version: '3.8'

services:
  rps-service:
    build: .
    image: rps-service:latest
    ports:
      - "8501:8501"
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    container_name: rps-service

networks:
  default:
    name: rps-network